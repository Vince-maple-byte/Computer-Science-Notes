The file to tell Spring how you want beans to be created. These instructions are typed in by either Annotation based configuration such as @Component, @Service, @Bean, etc. or by XML configuration.

#spring #java 